{
  "name": "dsmtech",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/dev.ts",
    "build": "bun src/build.ts",
    "preview": "bun run build && bun src/dev.ts --dist",
    "check": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "prettier": {
    "arrowParens": "always",
    "singleQuote": true,
    "tabWidth": 2,
    "trailingComma": "none"
  }
}
